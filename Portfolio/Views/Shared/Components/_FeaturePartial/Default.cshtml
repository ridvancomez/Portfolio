@model List<EntityLayer.Concrete.Feature>

@foreach (var item in Model)
{
    <section id="home" class="s-home page-hero target-section" data-parallax="scroll" data-image-src="/hola-master/images/hero-bg.jpg" data-natural-width=3000 data-natural-height=2000 data-position-y=center>

        <div class="overlay"></div>
        <div class="shadow-overlay"></div>

        <div class="home-content">

            <div class="row home-content__main">

                <h3>@item.Title</h3>

                <h1>
                    @item.Description
                </h1>

                <div class="home-content__buttons">
                    <a href="#works" class="smoothscroll btn btn--stroke">
                        Son Projelerim
                    </a>
                    <a href="#about" class="smoothscroll btn btn--stroke">
                        Hakkımda
                    </a>
                </div>

                <div class="home-content__scroll">
                    <a href="#about" class="scroll-link smoothscroll">
                        <span>Asagi Kaydirin</span>
                    </a>
                </div>

            </div>

        </div>

        <ul class="home-social">
            @foreach (var value in ViewBag.SocialMedia)
            {
                <li>
                    <a href="@value.Url"><i class="@value.Icon" aria-hidden="true"></i><span>@value.Name</span></a>
                </li>
            }

        </ul>

    </section>
}
